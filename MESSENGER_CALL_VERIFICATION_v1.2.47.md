# تقرير اختبار المراسلة العائمة والمكالمات — v1.2.47

**التاريخ:** 20 أغسطس 2026  
**النطاق:** الأيقونة العائمة للمراسلة، قائمة المستخدمين وحالة الاتصال، فتح المحادثة الفردية، الاتصال الصوتي والمرئي.  
**بيئة الفحص:** مصدر Android محلي باستخدام Java 17.

## النتيجة

نجحت المراجعة الساكنة لمسار الواجهة: الأيقونة العائمة تفتح شاشة المستخدمين، الضغط على المستخدم يمرر هوية الطرف إلى شاشة المحادثة، ورأس المحادثة يحتوي على زري الاتصال الصوتي والمرئي. كما أن مسار الاتصال يمرر نوع المكالمة إلى `LocalCallActivity`، حيث يختلف طلب الأذونات بحسب النوع: الميكروفون للمكالمة الصوتية، والميكروفون مع الكاميرا للمكالمة المرئية.

لم يظهر خلل مؤكد في الكود أو بوابات البناء. لم يمكن تنفيذ مكالمة WebRTC فعلية أو التحقق من نقطة الاتصال الخضراء على جهازين لأن بيئة البناء لا تحتوي على `adb` أو جهاز Android متصل.

| الاختبار | النتيجة | ملاحظة |
|---|---|---|
| فحص ربط الأيقونة العائمة بقائمة المستخدمين | ناجح ساكنًا | يحتاج تفاعلًا فعليًا لتأكيد موضعها وعدم حجبها لشريط التنقل. |
| فحص الضغط على المستخدم وفتح المحادثة | ناجح ساكنًا | يتم تمرير اسم وهوية المستخدم إلى شاشة المراسلة. |
| فحص عرض حالة الاتصال | ناجح ساكنًا | العلامة الخضراء مرتبطة بحالة الاتصال المحلية؛ يلزم جهازان أو جلسة presence فعلية. |
| فحص زر الاتصال الصوتي | ناجح ساكنًا | يمرر نوع الصوت ويطلب الميكروفون فقط. |
| فحص زر مكالمة الفيديو | ناجح ساكنًا | يمرر نوع الفيديو ويطلب الميكروفون والكاميرا. |
| `testDebugUnitTest` | نجح بدون مصادر اختبار | لا توجد حاليًا ملفات اختبار وحدة Kotlin، لذلك لا يمثل ذلك تغطية سلوكية. |
| `lintDebug` | ناجح | تقرير Lint أُنشئ دون فشل. |
| `assembleDebug` | ناجح | نسخة Debug بُنيت بنجاح. |
| `assembleRelease` | ناجح | نسخة Release بُنيت بنجاح. |
| توقيع APK | ناجح | `APK Signature Scheme v2` صحيح، بموقّع واحد. |
| اختبار جهاز Android فعلي | غير منفذ | `adb` غير متاح في بيئة الفحص. |

## سيناريو القبول على جهازين

يجب تثبيت النسخة على جهازين ضمن الشبكة المحلية نفسها وتسجيل الدخول بحسابين مختلفين. بعد فتح الشاشة الرئيسية، ينبغي أن تظهر أيقونة المراسلة العائمة دون تغطية شريط التنقل. عند الضغط عليها تظهر قائمة المستخدمين، ويظهر بجوار المستخدم المتصل مؤشر أخضر. عند الضغط على اسمه تفتح محادثته الفردية ويظهر اسمه في الرأس.

بعد ذلك يُختبر زر السماعة: ينبغي طلب الميكروفون فقط، ثم بدء الاتصال الصوتي بعد موافقة الطرفين، مع عمل الكتم والإنهاء وتسجيل المكالمة. ويُختبر زر الفيديو: ينبغي طلب الكاميرا والميكروفون، ثم تشغيل الفيديو مع إمكان إيقاف الكاميرا وكتم الميكروفون وإنهاء الاتصال. كما يجب اختبار الرفض والعودة من الإعدادات والتأكد من عدم بدء الوسائط تلقائيًا.

> نجاح البناء والفحص الساكن لا يثبت وحده نجاح WebRTC بين جهازين. الاعتماد النهائي على هذه الميزة يتطلب تنفيذ السيناريو السابق على جهازين Android فعليين داخل الشبكة المحلية.

## القرار

الحزمة جاهزة للاختبار التفاعلي، ولم تُجرَ تعديلات برمجية لأن الفحص الساكن وبوابات البناء لم يكشفا خللًا مؤكدًا. تبقى نتيجة المكالمات الفعلية وحالة الاتصال الخضراء **غير محسومة حتى اختبار الجهازين**.

## ملاحظات الموقع

لم يتم إنشاء أو تحديث موقع معاينة لهذا الاختبار، التزامًا بطلب إيقاف أعمال الموقع والتركيز على تطبيق Android المحلي.

**المؤلف:** Manus AI

## المراجع

لا توجد مصادر خارجية مستخدمة؛ النتائج مبنية على فحص مصدر المشروع وسجلات Gradle المحلية.
